services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: outehr-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:5000
    networks:
      - outehr-network
    restart: unless-stopped

networks:
  outehr-network:
    name: outehr-api_outehr-network
    external: true
